apiVersion: v1
kind: Service
metadata:
    name: "azehmwcluster-83f1"
    labels:
        app: "azehmwcluster-83f1"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "azehmwcluster-83f1"